<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="card text-bg-dark mb-3 my-5">
    <div class="card-body">
      <div class="d-flex justify-content-center">
        <div>
          <%= form.label :name, style: "display: block", class:"card-text" %>
          <%= form.text_field :name, class:"form-control" %>
          <%= form.label :client_id, style: "display: block", class:"card-text" %>
          <%= form.collection_select :client_id, @clients, :id, :name, prompt: true, class:"form-select" %>
        <div>
          <%= form.submit class:"btn button3 mt-3 mx-5" %>
        </div>
      </div>
    </div>
  </div>
<% end %>
